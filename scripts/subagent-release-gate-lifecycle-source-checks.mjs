import { sourceAnchorCheck, subagentPiToolsTestCoverage } from "./subagent-release-gate-source-check-helpers.mjs";

export function buildSubagentLifecycleSourceChecks(files) {
  return [
    sourceAnchorCheck({
      id: "source.close-capacity",
      label: "close_agent releases capacity only for inactive children",
      text: [
        files.projectStore,
        files.projectStoreSubagentFoundationTest,
        files.subagentPiTools,
        subagentPiToolsTestCoverage(files),
        files.subagentCloseAgent,
        files.subagentCloseAgentTest,
        files.subagentCloseAgentExecutor,
        files.subagentCloseAgentExecutorTest,
      ]
        .filter(Boolean)
        .join("\n"),
      anchors: [
        "close_agent released live sub-agent capacity while preserving transcript history",
        "CLOSE_BLOCKED_ACTIVE_STATUSES",
        "SUBAGENT_CLOSE_REQUEST_EVENT_TYPE",
        "SUBAGENT_CLOSE_RETAINED_HISTORY_MESSAGE",
        "SUBAGENT_CLOSE_AGENT_EXECUTOR_SCHEMA_VERSION",
        "resolveSubagentCloseAgentRequest",
        "assertCanCloseSubagentRun",
        "buildSubagentCloseRequestedRunEventPreview",
        "executeSubagentCloseAgent",
        "Cannot close active sub-agent",
        "refuses to close actively executing children before releasing capacity",
        "closes needs-attention children as abandoned work without deleting history",
        "blocks active children from closing but allows inactive or already-closed children",
        "builds close messages that preserve transcript and artifact expectations",
        "records close requests, releases capacity, and writes a retained-history child message",
        "replays existing close run events without repeating side effects",
        "capacityLeaseSnapshot.status",
        "transcript and artifacts are retained",
      ],
    }),
    sourceAnchorCheck({
      id: "source.lifecycle-hooks",
      label: "lifecycle hooks preserve transcript refs and bounded artifact pointers",
      text: [files.subagentLifecycleHooks, files.subagentLifecycleHooksTest, files.projectStore, files.projectStoreSubagentFoundationTest]
        .filter(Boolean)
        .join("\n"),
      anchors: [
        "SUBAGENT_LIFECYCLE_HOOK_SCHEMA_VERSION",
        "subagentLifecycleHookPreview",
        "subagentTranscriptPath",
        "subagent.lifecycle_started",
        "subagent.lifecycle_stopped",
        "subagent.lifecycle_closed",
        "parentTranscriptPath",
        "childTranscriptPath",
        "artifactPointers",
        "finalStatus",
        "records durable start transcript refs",
        "records stop artifact pointers and final status without copying result content",
        "records close time without deleting transcript refs",
        'not.toHaveProperty("summary")',
        'not.toHaveProperty("structuredOutput")',
      ],
    }),
    sourceAnchorCheck({
      id: "source.approval-bridge",
      label: "child approval bridge preserves child identity, scope, and parent wait resumption",
      text: [
        files.subagentApprovalBridge,
        files.subagentApprovalBridgeTest,
        files.subagentApprovalDecision,
        files.subagentApprovalDecisionTest,
        files.subagentIpc,
        files.subagentIpcTest,
        files.subagentParentClusterUiModel,
        files.subagentParentClusterUiModelTest,
        files.subagentParentClusterComponent,
        files.subagentParentClusterComponentTest,
        files.rendererApp,
        files.appModalHost,
        files.rendererStyles,
        files.piChildSessionAdapter,
        files.subagentWaitAgentExecutor,
        files.subagentWaitAgentExecutorTest,
        files.subagentPiTools,
        subagentPiToolsTestCoverage(files),
        files.agentRuntime,
        files.agentRuntimeTest,
        files.subagentIdempotency,
        files.subagentIdempotencyTest,
      ]
        .filter(Boolean)
        .join("\n"),
      anchors: [
        "SUBAGENT_APPROVAL_BRIDGE_SCHEMA_VERSION",
        "SUBAGENT_CHILD_APPROVAL_REQUEST_MAILBOX_TYPE",
        "SUBAGENT_PARENT_APPROVAL_REQUEST_MAILBOX_TYPE",
        "SUBAGENT_APPROVAL_RESPONSE_MAILBOX_TYPE",
        "SUBAGENT_PARENT_APPROVAL_FORWARDED_MAILBOX_TYPE",
        "resolveSubagentApprovalScope",
        "buildSubagentApprovalRequestBridgeDraft",
        "buildSubagentApprovalResponseBridgeDraft",
        "recordSubagentApprovalRequestBridgeIfNeeded",
        "recordSubagentApprovalResponseBridgeIfNeeded",
        "SUBAGENT_APPROVAL_RESOLUTION_SCHEMA_VERSION",
        "resolveSubagentApprovalDecision",
        "updateSubagentParentMailboxEventDeliveryState",
        "registerSubagentApprovalIpc",
        "subagents:resolve-approval",
        "subagents:cancel-run",
        "subagents:close-run",
        "Sub-agent child controls are disabled because ambient.subagents is off.",
        "ambient.subagents is off",
        "createSubagentApprovalRequestIdempotencyKey",
        "SubagentChildRuntimeApprovalRequest",
        "approvalRequests?: readonly SubagentChildRuntimeApprovalRequest[]",
        "approvalRequestRecords",
        "approvalResponseDeliveries",
        "resolveChildApprovalResponse",
        "pendingSubagentPermissionApprovalRequests",
        "native-permission-request",
        "subagent.approval_response.consumed",
        'operation: "approval-request"',
        'operation: "approval-response"',
        "forward_child_approval_then_wait",
        "resumeParentBlocking: true",
        "Child always grants default to this child thread",
        "narrows child always grants to the child thread by default",
        "builds child-attributed approval requests that return the parent to a wait barrier",
        "builds scoped approval responses that are sent to the child while preserving parent blocking",
        "records approval responses into the child mailbox and parent audit event idempotently",
        "records a UI approval response, consumes the parent request, and keeps the parent blocked",
        "resolves child approvals only when ambient.subagents is enabled",
        "SubagentParentClusterApprovalActionModel",
        "approvalActions",
        "childApprovalRequestActions",
        "labels child approval requests and forwarded decisions in the collapsed cluster",
        "resolveSubagentApprovalAction",
        "submitSubagentApprovalDecisionDialog",
        "subagentApprovalDecisionDialog",
        "SubagentApprovalDecisionDialog",
        "subagentApprovalScopeOptions",
        "Approve child request",
        "Deny child request",
        "Approval scope",
        "This child thread",
        "Parent thread tree",
        "Project/workspace",
        "subagent-approval-dialog",
        "subagent-approval-scope-list",
        "parentBlocker",
        "Blocking: approval",
        "Approval needed",
        "marks approval-blocked required children with warning blocker indicators",
        "records child approval requests and leaves the parent blocked on the child wait barrier",
        "delivers queued approval responses to the child runtime before waiting again",
        "leaves approval responses queued when no child approval-response resolver is attached",
        "surfaces child approval-response delivery evidence from wait_agent",
        "surfaces native child permission prompts as parent-forwarded approval requests",
        "round-trips native child permission prompts through parent approval and child resume",
        "waitTimedOutResolvesBarrier",
        "Child requested approval; parent approval was forwarded to the parent mailbox and the parent remains blocked on this child.",
        "Child approval response was delivered to the child runtime; the parent remains blocked until the child reaches a synthesis-safe result.",
      ],
    }),
    sourceAnchorCheck({
      id: "source.supervisor-request-bridge",
      label: "child supervisor requests preserve child identity without completing the child",
      text: [
        files.subagentSupervisorRequest,
        files.subagentSupervisorRequestTest,
        files.piChildSessionAdapter,
        files.subagentWaitAgentExecutor,
        files.subagentWaitAgentExecutorTest,
        files.subagentPiTools,
        subagentPiToolsTestCoverage(files),
        files.subagentPiToolResult,
        files.subagentPiToolResultTest,
        files.subagentParentClusterUiModel,
        files.subagentParentClusterUiModelTest,
        files.subagentIdempotency,
      ]
        .filter(Boolean)
        .join("\n"),
      anchors: [
        "SUBAGENT_SUPERVISOR_REQUEST_SCHEMA_VERSION",
        "SUBAGENT_CHILD_SUPERVISOR_REQUEST_MAILBOX_TYPE",
        "SUBAGENT_PARENT_SUPERVISOR_REQUEST_MAILBOX_TYPE",
        "SUBAGENT_SUPERVISOR_REQUEST_KINDS",
        "need_decision",
        "blocked",
        "progress_update",
        "marksChildComplete: false",
        'completionStatus: "not_complete"',
        "recordSubagentSupervisorRequestIfNeeded",
        "buildSubagentSupervisorRequestDraft",
        "createSubagentSupervisorRequestIdempotencyKey",
        'operation: "supervisor-request"',
        "SubagentChildRuntimeSupervisorRequest",
        "supervisorRequests?: readonly SubagentChildRuntimeSupervisorRequest[]",
        "supervisorRequestRecords",
        "supervisorRequestRecords: supervisorRequestRecords.map",
        "supervisorRequestAcknowledgement",
        "If wait_agent returns supervisorRequestRecords",
        "Child requested supervisor attention; parent mailbox records the request and the parent remains blocked until the child is synthesis-safe.",
        "Child sent a supervisor progress update; parent mailbox records the update while the parent keeps monitoring the child.",
        "records child supervisor requests without treating them as child completion",
        "builds child-attributed supervisor requests without marking the child complete",
        "records supervisor requests idempotently across child and parent mailbox events",
        "exposes child supervisor request records from wait_agent as compact Pi-visible handles",
        "follow-supervisor-request",
        "compacts singular child supervisor mailbox attribution for Pi-visible handles",
        "supervisorRequestActivity",
        "latestQueuedSupervisorRequestForRun",
        "Blocking: needs decision",
        "Supervisor request",
        "Child progress",
        "surfaces queued child supervisor requests as parent-blocking attention",
        "surfaces child supervisor progress updates without parent-blocking actions",
      ],
    }),
    sourceAnchorCheck({
      id: "source.startup-reconciliation",
      label: "startup reconciliation rehydrates lifecycle stop and restart events",
      text: [files.subagentStartupReconciliation, files.subagentStartupReconciliationTest].filter(Boolean).join("\n"),
      anchors: [
        "reconcileSubagentsOnRuntimeStartup",
        'subagentLifecycleEventType("SubagentStop")',
        "latestStartupRepairEvents",
        "onParentMailboxEventUpdated",
        "subagent.restart_reconciled",
        "subagent.lifecycle_interrupted",
        "emits repaired child run, child thread, lifecycle stop, restart event, and wait barrier updates",
        "emits only the latest startup repair lifecycle and restart events for repaired runs",
      ],
    }),
    sourceAnchorCheck({
      id: "source.parent-stop-cascade",
      label: "parent-stop cascade cancels dependent child mailbox work",
      text: [files.projectStore, files.projectStoreSubagentFoundationTest].filter(Boolean).join("\n"),
      anchors: [
        "cascadeSubagentParentRunStopped",
        "cancelPendingParentToChildMailboxEvents",
        "cancelledMailboxEventIds",
        "subagent.cancellation_cascade",
        "cascades a stopped parent run across dependent child runs, wait barriers, and pending mailbox work",
      ],
    }),
    sourceAnchorCheck({
      id: "source.parent-mailbox-anchoring",
      label: "parent mailbox failures stay anchored to visible parent messages",
      text: [
        files.sharedTypes,
        files.projectStore,
        files.projectStoreSubagentFoundationTest,
        files.subagentLifecycleParentMailbox,
        files.subagentLifecycleParentMailboxTest,
        files.subagentCancelAgent,
        files.subagentCancelAgentTest,
        files.subagentCancelAgentExecutor,
        files.subagentCancelAgentExecutorTest,
        files.agentRuntimeFinalizationBlocking,
        files.agentRuntimeFinalizationBlockingTest,
        files.agentRuntime,
        files.agentRuntimeTest,
        files.subagentPiTools,
        subagentPiToolsTestCoverage(files),
        files.subagentBatchJobs,
        files.subagentBatchJobsTest,
        files.subagentParentClusterUiModel,
        files.subagentParentClusterUiModelTest,
        files.mainIndex,
      ]
        .filter(Boolean)
        .join("\n"),
      anchors: [
        "parentMessageId?: string",
        "parent_message_id",
        "SUBAGENT_LIFECYCLE_INTERRUPTION_PARENT_MAILBOX_TYPE",
        "ambient-subagent-lifecycle-interruption-v1",
        "SUBAGENT_PARENT_CANCEL_REQUEST_SOURCE",
        "SUBAGENT_CANCEL_REQUEST_EVENT_TYPE",
        "SUBAGENT_CANCEL_AGENT_EXECUTOR_SCHEMA_VERSION",
        "buildSubagentCancelAgentParentMailboxDraft",
        "buildSubagentCancelRequestedRunEventPreview",
        "executeSubagentCancelAgent",
        "shouldPreserveInitialTerminalSubagentCancelRun",
        "listSubagentParentMailboxEventsForParentThread",
        "appendSubagentLifecycleInterruptionParentMailboxEvent",
        "onParentMailboxEventUpdated",
        "parentRun.assistantMessageId",
        "listSubagentParentMailboxEventsForParentThread(active)",
        "record.plan.parentMessageId",
        "run.parentMessageId",
        "recordSubagentFinalizationBlockedParentMailbox",
        "subagentFinalizationBarrierBlock",
        "subagentFinalizationBlockParentResolution",
        "subagentFinalizationBlockUserChoices",
        "parentFinalizationBlocked",
        '"parent_cancel_request"',
        '"direct_child_stop"',
        '"desktop_restart"',
        '"runtime_budget_exceeded"',
        "builds child-attributed lifecycle interruption payloads for direct stops and runtime budgets",
        "bounds lifecycle result artifacts before parent mailbox delivery",
        "uses source and explicit idempotency keys for lifecycle parent mailbox dedupe",
        "blocks parent finalization while required sub-agent wait barriers are unresolved",
        "blocks parent finalization after required sub-agent wait barriers resolve unsafe",
        "agent runtime finalization blocking helpers",
        "builds subagent finalization barrier blocks from unresolved required barriers",
        "records subagent finalization mailbox events with policy payloads",
        "persists parent mailbox parent-message anchors for pre-run spawn failures",
        "parentMessageId: assistant.id",
        'parentMessageId: "parent-message"',
        "settles runtime-budget overruns as aborted partial results when the role allows partial output",
        "settles runtime-budget overruns as failures when the role forbids partial output",
        "builds lifecycle parent mailbox drafts for parent cancel requests",
        "marks only active child runs as cancelled and preserves original terminal runs",
        "cancels active children, parent-to-child mailbox work, wait barriers, and records parent lifecycle evidence",
        "preserves terminal initial runs when runtime state is stale",
        "creates a parent cluster for anchored batch progress without child runs",
        "creates a parent cluster for anchored grouped completions without child run models",
        "creates an attention cluster for anchored child lifecycle interruptions without child runs",
        "creates a partial cluster for anchored runtime budget partials without child runs",
        "surfaces anchored parent-stop cascades as parent mailbox activity",
        "creates a parent cluster for anchored spawn failures without child runs",
      ],
    }),
    sourceAnchorCheck({
      id: "source.group-join",
      label: "background child completions use a typed grouped-notification contract",
      text: [
        files.subagentGroupJoin,
        files.subagentGroupJoinTest,
        files.subagentGroupedCompletionRecorder,
        files.subagentGroupedCompletionRecorderTest,
        files.projectStore,
        files.subagentPiTools,
        subagentPiToolsTestCoverage(files),
      ]
        .filter(Boolean)
        .join("\n"),
      anchors: [
        "SUBAGENT_GROUPED_COMPLETION_PARENT_MAILBOX_TYPE",
        "SUBAGENT_GROUPED_COMPLETION_SCHEMA_VERSION",
        "buildSubagentGroupedCompletionNotificationDraft",
        "createSubagentGroupedCompletionPayloadFingerprint",
        "upsertSubagentGroupedCompletionNotification",
        "SUBAGENT_GROUPED_COMPLETION_RECORDER_SCHEMA_VERSION",
        "recordSubagentGroupedCompletionNotificationIfNeeded",
        "updates an existing child completion in place without creating duplicate child rows",
        "rebatches straggler completions into the latest queued parent notification",
        "records optional background completions with bounded artifact summaries",
        "does not record required, active, or unsafe completed children",
      ],
    }),
    sourceAnchorCheck({
      id: "source.batch-exactly-once",
      label: "batch/fanout result reporting is typed and exactly-once before launch",
      text: [
        files.subagentBatchJobs,
        files.subagentBatchJobsTest,
        files.projectStore,
        files.projectStoreSchema,
        files.projectStoreSubagentFoundationTest,
      ]
        .filter(Boolean)
        .join("\n"),
      anchors: [
        "SUBAGENT_BATCH_RESULT_REPORT_SCHEMA_VERSION",
        "SUBAGENT_BATCH_RESULT_LEDGER_SCHEMA_VERSION",
        "SUBAGENT_BATCH_RESULT_LEDGER_VALIDATION_SCHEMA_VERSION",
        "validateSubagentBatchResultLedgerExactlyOnce",
        "invalid_ledger",
        "reportsByItemId",
        "reportIds",
        "PRIMARY KEY(job_id, report_id)",
        "idx_subagent_batch_result_reports_job_item_once",
        "accepts each item result exactly once and treats identical replay as a duplicate no-op",
        "validates exactly-once ledger invariants before accepting new reports",
        "rejects conflicting reports for an already reported item",
        "persists sub-agent batch jobs and exactly-once result ledgers",
      ],
    }),
    sourceAnchorCheck({
      id: "source.wait-barrier-synthesis",
      label: "wait barriers aggregate child synthesis safety by dependency mode",
      text: [
        files.projectStore,
        files.projectStoreSubagentFoundationTest,
        files.subagentPiTools,
        subagentPiToolsTestCoverage(files),
        files.subagentParentPolicyResolution,
        files.subagentParentPolicyResolutionTest,
        files.subagentWaitBarrierEvaluation,
        files.subagentWaitBarrierEvaluationTest,
        files.subagentTurnBudget,
        files.subagentTurnBudgetTest,
        files.subagentWaitContextResolver,
        files.subagentWaitContextResolverTest,
        files.subagentWaitAgentExecutor,
        files.subagentWaitAgentExecutorTest,
        files.subagentTurnBudgetWrapUpRecorder,
        files.subagentTurnBudgetWrapUpRecorderTest,
        files.subagentTurnBudgetExhaustionRecorder,
        files.subagentWaitMailbox,
        files.subagentWaitMailboxTest,
        files.subagentBarrierDecision,
        files.subagentBarrierDecisionTest,
        files.subagentBarrierDecisionRecorder,
        files.subagentBarrierDecisionRecorderTest,
        files.subagentBarrierControl,
        files.subagentBarrierControlTest,
        files.subagentBarrierControlExecutor,
        files.subagentBarrierControlExecutorTest,
        files.subagentBarrierDecisionExecutor,
        files.subagentBarrierDecisionExecutorTest,
      ]
        .filter(Boolean)
        .join("\n"),
      anchors: [
        "evaluateSubagentWaitBarrierForSynthesis",
        "SUBAGENT_WAIT_BARRIER_EVALUATION_SCHEMA_VERSION",
        "SUBAGENT_WAIT_BARRIER_TERMINAL_STATUSES",
        "SUBAGENT_WAIT_COMPLETION_SCHEMA_VERSION",
        "SUBAGENT_WAIT_BARRIER_ATTENTION_SCHEMA_VERSION",
        "SUBAGENT_WAIT_BARRIER_DECISION_SCHEMA_VERSION",
        "SUBAGENT_USER_DECISION_SCHEMA_VERSION",
        "SUBAGENT_WAIT_BARRIER_RESOLUTION_SCHEMA_VERSION",
        "SUBAGENT_WAIT_CONTEXT_RESOLVER_SCHEMA_VERSION",
        "SUBAGENT_WAIT_AGENT_EXECUTOR_SCHEMA_VERSION",
        "SUBAGENT_BARRIER_DECISION_RECORDER_SCHEMA_VERSION",
        "SUBAGENT_BARRIER_CANCEL_PARENT_SOURCE",
        "buildSubagentWaitCompletionMailboxDraft",
        "buildSubagentWaitBarrierAttentionParentMailboxDraft",
        "buildSubagentBarrierDecisionParentMailboxDraft",
        "recordSubagentBarrierDecisionParentMailbox",
        "buildSubagentBarrierDecisionResolutionArtifact",
        "buildSubagentBarrierDecisionRunEventPreview",
        "subagentBarrierDecisionNextStatus",
        "buildSubagentBarrierControlPlan",
        "buildSubagentBarrierControlRunPlan",
        "shouldMarkSubagentBarrierControlRunStatus",
        "buildSubagentBarrierCancelledMailboxPayload",
        "shouldRecordSubagentWaitCompletion",
        "shouldRecordSubagentWaitBarrierAttention",
        "SUBAGENT_BARRIER_CONTROL_EXECUTOR_SCHEMA_VERSION",
        "executeSubagentBarrierControlDecision",
        "SUBAGENT_BARRIER_DECISION_EXECUTOR_SCHEMA_VERSION",
        "executeSubagentBarrierDecision",
        "requiredSynthesisCountForBarrier",
        "waitBarrierStatusFromEvaluation",
        "SUBAGENT_PARENT_POLICY_RESOLUTION_SCHEMA_VERSION",
        "resolveSubagentParentPolicyForWait",
        "resolveSubagentParentPolicyForBarrierDecision",
        "allowedUserChoicesForSubagentWaitBarrier",
        "evaluateSubagentTurnBudgetForEvents",
        "compactSubagentTurnBudgetStateForPi",
        "turnBudgetState",
        "turnBudgetWrapUpSteering",
        "turnBudgetWrapUpDelivery",
        "turnBudgetExhaustionSettlement",
        "SUBAGENT_TURN_BUDGET_WRAP_UP_RECORDER_SCHEMA_VERSION",
        "recordSubagentTurnBudgetWrapUpSteeringIfNeeded",
        "shouldRecordSubagentTurnBudgetWrapUpSteering",
        "SUBAGENT_TURN_BUDGET_WRAP_UP_STEERING_REASON",
        "SUBAGENT_TURN_BUDGET_EXHAUSTION_RECORDER_SCHEMA_VERSION",
        "settleSubagentTurnBudgetExhaustionIfNeeded",
        "SUBAGENT_TURN_BUDGET_EXHAUSTED_EVENT_TYPE",
        "turn-budget-exhaustion",
        "followupChildRun",
        "wrap_up_due",
        "max_turns_exceeded",
        "resolveSubagentWaitContext",
        "findSubagentWaitBarrierForRuns",
        "executeSubagentWaitAgent",
        "waitBarrierEvaluation",
        "quorumThreshold",
        "waitBarrierMode",
        "childRunIds",
        "reuses matching aggregate barriers and preserves explicit quorum policy",
        "rejects aggregate waits across parent runs or with unrelated primary handles",
        "records completed waits after runtime completion and barrier resolution",
        "waits on the latest reservation state without fabricating output when no runtime is attached",
        "records durable turn-budget wrap-up steering while a required child is still running",
        "delivers turn-budget wrap-up steering to an attached child follow-up runtime",
        "keeps turn-budget wrap-up steering queued when the runtime cannot accept it yet",
        "queues one durable child follow-up when the turn budget reaches the wrap-up threshold",
        "replays existing wrap-up steering without duplicating mailbox or run events",
        "settles exhausted turn budget as aborted_partial without fabricating synthesis-safe output",
        "settles exhausted turn budget as failed when the role forbids partial output",
        "surfaces turn-budget wrap-up state in status_agent details",
        "surfaces turn-budget wrap-up steering evidence in wait_agent details",
        "surfaces turn-budget wrap-up runtime delivery evidence in wait_agent details",
        "surfaces turn-budget exhaustion settlement evidence in wait_agent details",
        "persists explicit quorum thresholds on wait barriers",
        "keeps required_all barriers blocked until every child has a synthesis-safe result",
        "allows required_any barriers from one validated child while preserving unsafe sibling provenance",
        "uses persisted quorum thresholds instead of implicit majority defaults",
        "creates Pi-reachable aggregate wait barriers with explicit quorum thresholds",
        "recordSubagentWaitBarrierAttentionParentMailboxIfNeeded",
        "records timed-out required wait barriers in the parent mailbox idempotently",
        "blocks parent synthesis while a required child barrier is still waiting",
        "requires user input before degrading a failed required barrier to partial",
        "keeps detach and cancel barrier decisions blocked and non-synthesizing",
        "uses explicit quorum thresholds and detects impossible quorum barriers",
        "resolves all-cancelled impossible barriers as cancelled",
        "resolves unsatisfied timed-out barriers as timed_out",
        "builds stable delivered wait-completion mailbox and run-event drafts",
        "builds compact queued parent attention mailbox drafts with allowed choices",
        "builds explicit partial resolution artifacts with user decision provenance",
        "builds cancel-parent resolution artifacts with control-state provenance",
        "builds parent mailbox drafts and replays control state from existing artifacts",
        "records a delivered parent mailbox event for explicit barrier control state",
        "replays persisted control state from the barrier resolution artifact",
        "plans cancel-parent runtime cancellation with stable idempotency and source metadata",
        "marks only active mismatched statuses after runtime cancellation returns",
        "cancels active children through the runtime and cancels pending parent-to-child mailbox work",
        "does not overwrite terminal runtime results during cancel-parent control",
        "records partial barrier decisions across barrier, child, and parent mailbox evidence",
        "replays existing barrier decisions without repeating child side effects",
        "subagent.wait_barrier_decision",
        "detach_child",
        "cancel_parent",
        "records detach and parent-cancel barrier decisions with child state changes",
      ],
    }),
    sourceAnchorCheck({
      id: "source.subagent-target-resolver",
      label: "path-addressed sub-agent target resolution is parent-bounded and test-covered",
      text: [
        files.subagentTargetResolver,
        files.subagentTargetResolverTest,
        files.subagentPiTools,
        files.subagentWaitContextResolver,
        files.subagentWaitContextResolverTest,
      ]
        .filter(Boolean)
        .join("\n"),
      anchors: [
        "SUBAGENT_TARGET_RESOLVER_SCHEMA_VERSION",
        "ambient-subagent-target-resolver-v1",
        "resolveSubagentTargetRun",
        "resolveSubagentTargetWaitBarrier",
        "assertSubagentRunOpenForAction",
        "childRunId, agentId, or canonicalTaskPath must identify an existing sub-agent run.",
        "does not belong to the current parent thread",
        "No sub-agent wait barrier exists for child run",
        "resolves target runs by childRunId, agentId, and canonical task path within the parent thread",
        "resolves explicit wait barriers and latest barriers for target child runs",
        "blocks actions against closed or terminal sub-agent runs",
      ],
    }),
    sourceAnchorCheck({
      id: "source.local-text-runtime-policy",
      label: "local text sub-agents enforce local runtime lifecycle, memory, and invocation limits",
      text: [
        files.localTextDelegation,
        files.localTextDelegationTest,
        files.localTextSubagentRuntime,
        files.localTextSubagentRuntimeTest,
        files.localModelRuntimeManager,
        files.localModelRuntimeManagerTest,
        files.localModelResourceRegistry,
        files.localModelResourceRegistryTest,
        files.localTextSubagentStartupConfig,
        files.localTextSubagentStartupConfigTest,
        files.agentRuntime,
        files.agentRuntimeTest,
        files.subagentPiTools,
        subagentPiToolsTestCoverage(files),
        files.subagentThreadInspectorUiModel,
        files.subagentThreadInspectorUiModelTest,
      ]
        .filter(Boolean)
        .join("\n"),
      anchors: [
        "prepareLocalTextDelegationRuntimePlan",
        "enforceLocalModelResourceLaunchPolicy",
        "validateLocalModelResourcePolicySnapshot",
        "ambient-local-model-resource-policy-validation-v1",
        "requestedLaunch",
        "resourcePolicyEnforcement",
        "invocationLimits",
        "ambient-local-text-delegation-invocation-limits-v1",
        "launchReadiness",
        "ambient-local-text-runtime-launch-readiness-v1",
        "preflightChildLaunch",
        "ambient-subagent-child-runtime-launch-preflight-v1",
        "runtime_launch_preflight",
        "buildResourceRegistryForLaunch",
        "residentDetection",
        "localTextStateRootPath",
        "Sub-agent capacity preflight failed",
        'failureStage: "capacity"',
        "killLocalModelProcess",
        "max_tokens",
        "outputValidation",
        "ambient-local-text-output-validation-v1",
        "requiresFullOutputArtifact",
        "unloaded-idle",
        "completeLocalTextDelegation",
        "createLocalTextSubagentRuntimeAdapter",
        "LocalModelRuntimeManager",
        "LocalModelRuntimeStartupError",
        "ambient-local-model-runtime-startup-failure-v1",
        "probeLocalModelRuntimeHealth",
        "subagent.local_text_runtime_failed",
        "blocks local invocations that cannot fit the model context window",
        "does not acquire a runtime when requested local output exceeds the model max output limit",
        "does not acquire a runtime when the local launch descriptor is malformed",
        "records pre-run spawn failures for local runtime launch preflight denials",
        "preflights local launch readiness before runtime start",
        "fails before acquiring the runtime when the local prompt exceeds model context limits",
        "fails before acquiring the runtime when the local launch descriptor is malformed",
        "records structured runtime startup failure evidence in the visible child thread",
        "throws structured startup failure evidence when health never becomes ready",
        "unloads idle local model runtimes before acquiring when memory policy requires cleanup",
        "unloads idle local runtimes and records enforcement before the visible child runs",
        "discovers Ambient-managed local text runtime state through detector options",
        "preflights custom local text runtime state roots as active memory evidence",
        "unloads idle candidates before launch when unload-idle behavior is configured",
        "validates local-memory policy snapshots before launch enforcement",
        "rejects policy decisions that understate snapshotted requested launch memory",
        "blocks local launch capacity when the resource policy snapshot contradicts its memory ceiling",
        "localTextSubagentStartupFeatureFromEnv",
        "resolveRuntimeForMain",
        "selectableAsMain: true",
        'runtime: "local_text"',
        "routes configured local text main chat through the local runtime without Pi",
        "Full local text output:",
        "preserves local text output validation evidence with completion results",
        "records local text output validation evidence in completed run events",
        "ambient-local-model-runtime-acquisition-v1",
        "runtimeAcquisition",
        "compactLocalModelRuntimeAcquisition",
        "Local runtime acquisition",
        'source: "persisted"',
        'source: "active"',
        "reuses a healthy persisted runtime after manager recreation",
        "shows local runtime lease state rows from completed local text events",
        "compactLocalModelRuntimeState",
        "runtimeState",
        "compactLocalModelRuntimeRelease",
        "runtimeRelease",
        "ambient-local-model-runtime-release-v1",
        "Local runtime release",
        "releasedAt",
        "idleCleanupDueAt",
        "localModelRuntimeIdleCleanupDueAt",
        "returns idle cleanup timing when the final lease is released but the runtime stays warm",
        "cleanup due",
        "still-leased",
        "keeps a reused local runtime alive until the final lease is released",
        "records still-leased runtime evidence in completed local text events",
        "shows still-leased local runtime release evidence in inspector rows",
        "releaseLocalTextRuntimeLease",
        'status: "failed"',
        "release store unavailable",
        "preserves completed local text output when runtime release fails",
        "records completed local text events when runtime release fails",
        "shows local runtime release failures in inspector rows",
        "LocalTextDelegationRuntimeFailureError",
        "ambient-local-text-delegation-failure-v1",
        "subagent.local_text_release_after_failure",
        "records runtime release evidence when local text completion fails after acquire",
        "shows local runtime release evidence from failed local text events",
        "appendTerminalLocalTextReleaseEvidence",
        "ambient-local-text-terminal-release-v1",
        "subagent.local_text_release_after_cancel",
        "subagent.local_text_release_after_partial",
        'terminalStatus: "failed"',
        "Local text runtime lease released after the child reached failed.",
        "shows terminal local runtime release evidence from strict budget failures",
        "does not let a late local completion overwrite parent cancellation",
        "shows local runtime release evidence from cancelled local text events",
        "Local text runtime lease released after the child was cancelled.",
        "localTextOutputValidationPreview",
        "Text output valid",
        "full artifact required",
        "shows local text output validation evidence in recent events",
        "localTextRuntimePreflightPreview",
        "localTextResourcePolicyEnforcementLabel",
        "Context:",
        "shows local text runtime preflight evidence in recent events",
        "localRuntimeRowsForRun",
        "Local runtime memory",
        "shows local runtime lease state rows from completed local text events",
        "AMBIENT_LOCAL_TEXT_SUBAGENT_COMMAND",
        "AMBIENT_LOCAL_TEXT_SUBAGENT_COMPLETION_URL",
        "builds an available local text profile and runtime descriptor from startup env",
        "does not enable local profiles for partial or invalid descriptors",
      ],
    }),
    sourceAnchorCheck({
      id: "source.local-runtime-lifecycle-ownership",
      label: "local runtime lifecycle tools honor sub-agent ownership leases",
      text: [
        files.localRuntimeInventory,
        files.localRuntimeInventoryTest,
        files.localModelRuntimeManager,
        files.localModelRuntimeManagerTest,
        files.localModelRuntimeStatus,
        files.localModelRuntimeStatusTest,
        files.localModelRuntimeStart,
        files.localModelRuntimeStartTest,
        files.localModelRuntimeStop,
        files.localModelRuntimeStopTest,
        files.localModelRuntimeRestart,
        files.localModelRuntimeRestartTest,
        files.agentRuntimeLocalRuntimeTools,
        files.agentRuntimeLocalRuntimeToolsTest,
        files.localRuntimeOwnershipResolution,
      ]
        .filter(Boolean)
        .join("\n"),
      anchors: [
        "buildLocalRuntimeInventory",
        "ambient-local-runtime-inventory-v1",
        "buildLocalRuntimePolicyHandoff",
        "localRuntimeLifecycleDecision",
        "LocalModelRuntimeLeaseRecoverySummary",
        "ambient-local-runtime-lease-recovery-v1",
        "readRepairedLocalModelRuntimeLeaseJournalsWithRecovery",
        "leaseRecovery",
        "Lease recovery:",
        "joins active sub-agent leases to local runtime rows and blocks ordinary Stop",
        "treats active-looking sub-agent leases as stale only when a freshness window is supplied",
        "joins active sub-agent leases into read-only runtime inventory stop blockers",
        "reports stale persisted lease recovery when a dead runtime pid is repaired to crashed",
        "surfaces stale lease evidence without blocking ordinary Stop",
        "repairs dead persisted runtime owner leases as crashed status evidence",
        "actions Stop disabled, Restart disabled, Start disabled, Unload disabled",
        "planLocalModelRuntimeStart",
        "ambient-local-model-runtime-start-plan-v1",
        "blocks active sub-agent leases and reports the load decision reason",
        "blocks Start when the target load violates local memory policy",
        "blocks untracked local model processes",
        "planLocalModelRuntimeStop",
        "ambient-local-model-runtime-stop-plan-v1",
        "blocks active sub-agent leases and explains force requirements",
        "blocks malformed active owner leases without offering forced Stop",
        "planLocalModelRuntimeRestart",
        "ambient-local-model-runtime-restart-plan-v1",
        "blocks Restart when reloading a stopped runtime violates local memory policy",
        "blocks malformed active owner leases without offering forced Restart",
        "createLocalRuntimeToolExtension",
        "ambient_local_model_runtime_start",
        "ambient_local_model_runtime_stop",
        "ambient_local_model_runtime_restart",
        "runs provider-declared Start, Stop, and Restart for voice runtime rows",
        "resolves forced provider-declared Stop ownership before stopping a sub-agent owned runtime",
        "localRuntimeOwnershipResolutionRequest",
        "localRuntimeOwnershipResolutionAfterInventoryRefresh",
        "cancel-or-mark-affected-subagents",
      ],
    }),
  ];
}
